version: '3.8'

services:
  incub8-website:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    container_name: incub8-website
    restart: unless-stopped
    environment:
      - NODE_ENV=production
