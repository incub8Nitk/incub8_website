# Dependencies and build output
node_modules
.dist
/dist

# Logs and coverage
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
coverage

# VCS and editor
.git
.gitignore
.vscode
.idea
.DS_Store
*.swp

# Docker-related (do not send these to the build context)
.dockerignore
.docker

# Env files: keep .env available for Vite build if needed
# Do not send local overrides
.env.*.local

# Archives and misc
*.zip
*.tar
*.tar.gz
